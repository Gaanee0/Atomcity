---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

type: files
files:
  - source: shared/usr
    destination: /usr
  - source: shared/etc
    destination: /etc
  - source: catalyst/usr
    destination: /usr
  - source: catalyst/etc
    destination: /etc

